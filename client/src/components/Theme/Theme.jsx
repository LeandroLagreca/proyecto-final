import { createTheme } from '@mui/material/styles';

const theme = createTheme({
	palette: {
		primary: {
		  main: "#1b2838",
          light: "#2a475e",
          dark: "#171a21",

		},
		secondary: {
		  main: "#66c0f4",
          light: "#c7d5e0"
		},
	  },
});


export default theme;